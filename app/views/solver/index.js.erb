$('#spinner').hide();

<%if @solved_words.present?%>
  $('#solved_words').append("<%=escape_javascript(
    render(
      :partial => "words/result",
      :collection => @solved_words,
      :as => :word))%>");
<%else%>
  $('#no_matches_found').show();
<%end%>
